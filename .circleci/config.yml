version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
          name: system.testing
          command: |
            apt-get install --no-install-recommends --no-install-suggests -y \
                python3 \
                python3-dev \
                python3-pip 
            pip3 instal setuptools \
            pip3 install docker \
            pip3 install docker-compose \
            ./ci/do.ci system.testing

