version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
          name: system.testing
          command: |
            pyenv global 3.5.2
            python3 --version
            pip3.5 --version
            pip3.5 install setuptools
            pip3.5 install docker
            pip3.5 install docker-compose
            sudo mkdir /test-log
            sudo chmod a+rwx /test-log
            export LOG_DIR=$HOME/test-log
            ./ci/do_ci.sh system.testing
      - store_artifacts:
          path: /test-log

