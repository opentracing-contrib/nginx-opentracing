version: 2
jobs:
  build:
    machine:
      python:
        version: 3.5.2
      pre: pyenv global 3.5.2
    steps:
      - checkout
      - run:
          name: system.testing
          command: |
            python3 --version
            pip --version
            sudo pip install setuptools
            sudo pip install docker
            sudo pip install docker-compose
            ./ci/do_ci.sh system.testing

