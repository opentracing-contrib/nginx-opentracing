version: 2
jobs:
  build:
    machine:
      post: pyenv global 3.6.0
    steps:
      - checkout
      - run:
          name: system.testing
          command: |
            sudo pip3 install setuptools
            sudo pip3 install docker
            sudo pip3 install docker-compose
            python3 --version
            ./ci/do_ci.sh system.testing

